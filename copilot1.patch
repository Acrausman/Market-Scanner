// ✅ Apply split and dividend adjustments
- await ApplyCorporateActionsAsync(symbol, bars);
+ 
+ // 🔍 Check if Polygon data is already adjusted
+ bool isAdjusted = true;
+ try
+ {
+     string splitUrl = $"https://api.polygon.io/v3/reference/splits?ticker={symbol}&apiKey={_apiKey}";
+     var splitResp = await splitUrl.GetJsonAsync<JObject>();
+     var splits = splitResp["results"]?.ToList() ?? new List<JToken>();
+     isAdjusted = DetectIfPolygonDataIsAdjusted(bars, splits);
+ 
+     if (!isAdjusted)
+     {
+         Logger.WriteLine($"[Polygon] {symbol}: detected unadjusted data — applying corporate actions...");
+         await ApplyCorporateActionsAsync(symbol, bars);
+     }
+     else
+     {
+         Logger.WriteLine($"[Polygon] {symbol}: data already adjusted by Polygon, skipping manual adjustments.");
+     }
+ }
+ catch (Exception ex)
+ {
+     Logger.WriteLine($"[Polygon] {symbol}: adjustment check failed ({ex.Message}), assuming adjusted=true.");
+ }
